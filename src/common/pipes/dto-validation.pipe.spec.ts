import { CustomDtoValidationPipe } from './dto-validation.pipe';

describe('DtoValidationPipePipe', () => {
	it('should be defined', () => {
		expect(new CustomDtoValidationPipe()).toBeDefined();
	});
});
